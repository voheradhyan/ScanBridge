<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net8.0-windows</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <LangVersion>latest</LangVersion>
    <TreatWarningsAsErrors>true</TreatWarningsAsErrors>
    <AssemblyName>RemoteScanner.Service</AssemblyName>
    <RootNamespace>RemoteScanner.Service</RootNamespace>
    <PlatformTarget>x64</PlatformTarget>
    <InvariantGlobalization>true</InvariantGlobalization>
  </PropertyGroup>

  <ItemGroup>
    <!-- ServiceBase, for the session-change notifications the whole design depends on. -->
    <PackageReference Include="System.ServiceProcess.ServiceController" Version="8.0.1" />
  </ItemGroup>

  <!-- The two data sources travel inside this executable and are written into the system TWAIN
       folders when it installs itself. A TWAIN manager looks nowhere else, so they cannot stay
       resources, but the thing a person downloads is still one file.

       Conditional because a plain "dotnet build" without the native build having run should
       still compile; EmbeddedPayload.Extract then reports precisely what is missing and how to
       produce it, rather than failing at a null stream. -->
  <ItemGroup>
    <EmbeddedResource Include="..\..\build\x64\RemoteScanner.ds"
                      Condition="Exists('..\..\build\x64\RemoteScanner.ds')"
                      LogicalName="RemoteScanner.Payload.x64/RemoteScanner.ds" />
    <EmbeddedResource Include="..\..\build\x86\RemoteScanner.ds"
                      Condition="Exists('..\..\build\x86\RemoteScanner.ds')"
                      LogicalName="RemoteScanner.Payload.x86/RemoteScanner.ds" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\RemoteScanner.Common\RemoteScanner.Common.csproj" />
    <ProjectReference Include="..\RemoteScanner.Rdp\RemoteScanner.Rdp.csproj" />
    <!-- The session-agent role, hosted by this executable rather than shipped as its own. -->
    <ProjectReference Include="..\RemoteScanner.SessionAgent\RemoteScanner.SessionAgent.csproj" />
  </ItemGroup>

</Project>
