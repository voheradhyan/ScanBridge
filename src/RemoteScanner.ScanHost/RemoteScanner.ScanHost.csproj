<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net8.0-windows</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <LangVersion>latest</LangVersion>
    <TreatWarningsAsErrors>true</TreatWarningsAsErrors>
    <AssemblyName>RemoteScanner.ScanHost</AssemblyName>
    <RootNamespace>RemoteScanner.ScanHost</RootNamespace>
    <!-- Published twice, x86 and x64, so a data source of either bitness can be loaded.
         See docs/01-FEASIBILITY.md section 5. -->
    <Platforms>x64;x86</Platforms>
    <RuntimeIdentifiers>win-x64;win-x86</RuntimeIdentifiers>
    <InvariantGlobalization>true</InvariantGlobalization>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\RemoteScanner.Common\RemoteScanner.Common.csproj" />
    <ProjectReference Include="..\RemoteScanner.Protocol\RemoteScanner.Protocol.csproj" />
    <ProjectReference Include="..\RemoteScanner.Scanner\RemoteScanner.Scanner.csproj" />
  </ItemGroup>

</Project>
