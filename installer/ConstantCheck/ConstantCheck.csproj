<Project Sdk="Microsoft.NET.Sdk">

  <!-- Deliberately outside ScanBridge.sln: this is a build gate, not a component, and
       nothing ships it. Build-All.ps1 builds it, then invokes the compiled DLL directly. -->

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net8.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <TreatWarningsAsErrors>true</TreatWarningsAsErrors>
    <AssemblyName>ScanBridge.ConstantCheck</AssemblyName>
    <RootNamespace>ScanBridge.ConstantCheck</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="System.Reflection.MetadataLoadContext" Version="8.0.0" />
  </ItemGroup>

</Project>
